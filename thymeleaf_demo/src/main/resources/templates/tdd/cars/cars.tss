ul.car-makes {
/*	th-if: "*{carMakes?.size() > 0}"; */
/*	th-remove: "all-but-first"; */
}

ul.car-makes > li {
/*	th-each: "carMake: *{carMakes}"; */
}

ul.car-makes > li > h2 {
/*	th-text: "${carMake.name}"; */
}

ul.car-models {
/*	th-if: "${carMake.models?.size() > 0}"; */
/*	th-remove: "all-but-first"; */
}

ul.car-models > li {
/*	th-each: "carModel: ${carMake.models}"; */
/*	th-text: "${carModel.name}"; */
}
